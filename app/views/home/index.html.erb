    <header>
      <nav class="nav">
        <ul>
      <% if user_signed_in? %>
        <li><%= link_to "Logout", destroy_user_session_path, :method => :delete %></li>
      <% else %>
        <li><%= link_to 'Sign In!', new_user_session_path, class: "signin" %></li>
      <% end %>
        <li><%= link_to "Calendar", calendars_path %></li>
        <li><%= link_to "Root", root_path, class: "current" %></li>
        <li><%= link_to "My Groups", groups_path %></li>
        <li><%= link_to "Users", users_path %></li>
        </ul>
      </nav>
    </header>

<h1>Homie Page</h1>

<% if @user && @user.role?(:god) %>
  <h3>Greetings, Your Majesty</h3>
<% elsif @user && @user.role?(:king) %>
  <h3>Welcome, Group Admin</h3>
<% elsif @user && @user.role ==('man') %>
  <h3>Welcome, noob</h3>
<% end %>

hi