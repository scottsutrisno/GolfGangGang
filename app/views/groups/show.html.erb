    <header>
      <nav class="nav">
        <ul>
      <% if user_signed_in? %>
        <li><%= link_to "Logout", destroy_user_session_path, :method => :delete %></li>
      <% else %>
        <li><%= link_to 'Sign In!', new_user_session_path, class: "signin" %></li>
      <% end %>
        <li><%= link_to "Calendar", calendars_path %></li>
        <li><%= link_to "Weather", root_path %></li>
        <li><%= link_to "My Groups", groups_path, class: "current" %></li>
        <li><%= link_to "Users", users_path %></li>
        </ul>
      </nav>
    </header>
<div class="devforms2">
    <h1>Groups</h1>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">Name</th>
        <th scope="col">Avatar</th>
        <th scope="col">Status</th>
        <th scope="col">Groups</th>
      </tr>
    </thead>
    <tbody>

      <tr>
        <td><%= @group.name %></td>
        <td><img src="<%= @group.avatar %>"></td>
        <td><%= @group.status %></td>
        <td class="grp"><%= link_to "Edit", edit_group_path(@group) %></td>
        <td class="grp"><%= link_to 'Delete', @group, method: :delete, data: { confirm: 'Are you Sure?' } %></td>

      </tr>
    </tbody>
  </table>



<h2>Members:</h2>


    <% @group.users.each do |user|%>
        <p>Username: <%= user.username %> - <%= user.first_name %><%= user.last_name %></p>
    <% end %>
</div>