    <header>
      <nav class="nav">
        <ul>
      <% if user_signed_in? %>
        <li><%= link_to "Logout", destroy_user_session_path, :method => :delete %></li>
      <% else %>
        <li><%= link_to 'Sign In!', new_user_session_path %></li>
      <% end %>
        <li><%= link_to "Calendar", calendars_path %></li>
        <li><%= link_to "Root", root_path %></li>
        <li><%= link_to "My Groups", groups_path, class: "current" %></li>
        <li><%= link_to "Users", users_path %></li>
        </ul>
      </nav>
    </header>



<h1>Groups</h1>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">Name</th>
        <th scope="col">Avatar</th>
        <th scope="col">Status</th>
        <th scope="col">Groups</th>
      </tr>
    </thead>
    <tbody>
    <% @groups.each do |group| %>
      <tr>
        <td><%= group.name %></td>
        <td><img src="<%= group.avatar %>"></td>
        <td><%= group.status %></td>
        <td class="btn btn-dark"><%= link_to "Show", group %></td>
        <td class="btn btn-dark"><%= link_to "Edit", edit_group_path(group) %></td>
        <td class="btn btn-danger"><%= link_to 'Delete', group, method: :delete, data: { confirm: 'Are you Sure?' } %></td>

      </tr>
      <% end %>
    </tbody>
  </table>
  <button type="button" <%= link_to 'New Group', new_group_path %></button>


